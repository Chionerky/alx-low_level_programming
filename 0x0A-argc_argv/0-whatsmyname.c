#include <stdio.h>

/**
 * main - write program to prints name, followed by new line
 * rename the program to print new name
 *
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: return 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
