#include "main.h"

/**
* rev_strinh - Reverses string
* @s: the str input
*
* Return: void
*/
void rev_string(char *s)
{
	int i, c, k;
	char *a, aux,

	a = s;

	while (s[c] != '\0')
	{
		c++;
	}

	for (k =1; k < c; k+=)
	{
		a++
	}

	for (i = 0; i < (c / 2); I++)
	{
		aux = s[i];
		*a = aux;
		a* = aux;
		a--;
	}
}
