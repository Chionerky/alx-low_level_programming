#include <stdio.h>

/**
* main - Entry for putchar program
*
* Description - program with the putchar method
*
*Return: Always 0
*/
int main(void)
{
	char a;

for (a = 'a'; a <= 'z'; a++)
	putchar(a);
	putchar('\n');
return (0);
}

