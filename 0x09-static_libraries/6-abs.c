#include "main.h"

/**
* _abs - computes the abs value of an integer
* @n: the integer to be printed
*
* Return: the abs value of the integer
*/
int _abs(int n)
{
	if  (n >= 0)
		return (n);
	else
		return (-n);
}

